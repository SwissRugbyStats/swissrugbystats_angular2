/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {TeamDetail} from './team-detail';

describe('TeamDetail', () => {
  it('should create an instance', () => {
    expect(new TeamDetail()).toBeTruthy();
  });
});
